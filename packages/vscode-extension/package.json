{
  "name": "contextmover-vscode",
  "displayName": "ContextMover",
  "description": "Captures IDE context and bridges it to AI coding agents",
  "version": "0.1.0",
  "publisher": "ContextMover",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "contextmover.captureContext",
        "title": "ContextMover: Capture Current Context"
      },
      {
        "command": "contextmover.copyContextForLLM",
        "title": "ContextMover: Copy IDE Context For LLM"
      },
      {
        "command": "contextmover.openInLLM",
        "title": "ContextMover: Open LLM With IDE Context"
      },
      {
        "command": "contextmover.startBridge",
        "title": "ContextMover: Start Bridge Server"
      }
    ],
    "configuration": {
      "title": "ContextMover",
      "properties": {
        "contextmover.bridgePort": {
          "type": "number",
          "default": 49152,
          "description": "Local HTTP port for the browser extension bridge"
        },
        "contextmover.maxFilesInContext": {
          "type": "number",
          "default": 10,
          "description": "Max open files to include in context snapshots"
        },
        "contextmover.maxWorkspaceFilesInContext": {
          "type": "number",
          "default": 30,
          "description": "Max workspace files to index into the migrated IDE context"
        },
        "contextmover.maxWorkspaceFileChars": {
          "type": "number",
          "default": 1200,
          "description": "Max characters to capture per indexed workspace file"
        },
        "contextmover.preferredProvider": {
          "type": "string",
          "default": "ask",
          "enum": [
            "ask",
            "chatgpt",
            "claude",
            "gemini",
            "grok",
            "custom"
          ],
          "description": "Default LLM target to receive IDE context migrations"
        },
        "contextmover.customProviderUrl": {
          "type": "string",
          "default": "",
          "description": "Optional HTTPS URL to open when the preferred provider is set to custom"
        }
      }
    }
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "package": "vsce package"
  },
  "dependencies": {
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.90.0",
    "@types/ws": "^8.5.10",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^2.29.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5"
  }
}